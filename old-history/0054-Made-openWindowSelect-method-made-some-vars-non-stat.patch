From 196190b3556ec95fcb22100f463a39604cd2a07d Mon Sep 17 00:00:00 2001
From: Kevin Thorne <kevint@aptitekk.com>
Date: Tue, 22 Jul 2014 10:41:22 -0600
Subject: [PATCH 054/101] Made openWindowSelect method, made some vars
 non-static for security.

---
 .../aptitekk/TeamScreen/Frames/MainFrame.java | 39 ++++++++++++++++---
 .../TeamScreen/Frames/WindowSelect.java       |  2 +-
 .../aptitekk/TeamScreen/TeamScreenDaemon.java | 25 ++++++------
 3 files changed, 48 insertions(+), 18 deletions(-)

diff --git a/TeamScreen/src/com/aptitekk/TeamScreen/Frames/MainFrame.java b/TeamScreen/src/com/aptitekk/TeamScreen/Frames/MainFrame.java
index 3648549..97ce4c8 100644
--- a/TeamScreen/src/com/aptitekk/TeamScreen/Frames/MainFrame.java
+++ b/TeamScreen/src/com/aptitekk/TeamScreen/Frames/MainFrame.java
@@ -9,10 +9,13 @@ import java.awt.Image;
 import java.awt.Point;
 import java.awt.event.ActionEvent;
 import java.awt.event.ActionListener;
+import java.awt.event.FocusEvent;
+import java.awt.event.FocusListener;
 import java.awt.event.MouseEvent;
 import java.awt.event.MouseListener;
 import java.awt.event.MouseMotionListener;
 import java.awt.event.WindowEvent;
+import java.awt.event.WindowFocusListener;
 
 import javax.swing.BorderFactory;
 import javax.swing.Box;
@@ -48,6 +51,7 @@ public class MainFrame extends JFrame implements ActionListener, MouseListener{
 	ImageButton exitPane;
 	
 	boolean pinned = false;
+	boolean inMeeting = false;
 	
 	class ImageButton{
 		JLabel image;
@@ -88,8 +92,8 @@ public class MainFrame extends JFrame implements ActionListener, MouseListener{
 		
 		System.setProperty("com.apple.mrj.application.apple.menu.about.name", "TeamScreen");
 		this.setTitle("TeamScreen");
-		this.setMaximumSize(new Dimension(430, 70));
-		this.setMinimumSize(new Dimension(430, 70));
+		this.setMaximumSize(new Dimension(480, 70));
+		this.setMinimumSize(new Dimension(480, 70));
 		setResizable(false);
 		setLocationRelativeTo(null);
 
@@ -100,7 +104,7 @@ public class MainFrame extends JFrame implements ActionListener, MouseListener{
 	    mouseDownCompCoords = null;
         this.setUndecorated(true);
         this.setVisible(true);
-        this.setBounds(0, 0, 430, 70);
+        this.setBounds(0, 0, 480, 70);
         this.setAlwaysOnTop(pinned);
 
         this.addMouseListener(new MouseListener(){
@@ -134,6 +138,24 @@ public class MainFrame extends JFrame implements ActionListener, MouseListener{
 		((JPanel) this.getContentPane()).setBorder(BorderFactory.createCompoundBorder(
 				BorderFactory.createMatteBorder(5, 5, 5, 5, new Color(0, 120, 255)),
 				new EmptyBorder(10, 10, 10, 10)));
+		this.addWindowFocusListener(new WindowFocusListener() {
+
+			@Override
+			public void windowGainedFocus(WindowEvent e) {
+				((JPanel) getContentPane()).setBorder(BorderFactory.createCompoundBorder(
+						BorderFactory.createMatteBorder(5, 5, 5, 5, new Color(0, 120, 255)),
+						new EmptyBorder(10, 10, 10, 10)));
+			}
+
+			@Override
+			public void windowLostFocus(WindowEvent e) {
+				if(!pinned)
+					((JPanel) getContentPane()).setBorder(BorderFactory.createCompoundBorder(
+							BorderFactory.createMatteBorder(5, 5, 5, 5, new Color(138, 138, 138)),
+							new EmptyBorder(10, 10, 10, 10)));
+			}
+			
+		});
 		
 		this.pack();
 	}
@@ -155,7 +177,7 @@ public class MainFrame extends JFrame implements ActionListener, MouseListener{
 		mainPane.add(leavePane.getIconPane());
 		
 		//space
-		mainPane.add(Box.createHorizontalStrut(75));
+		mainPane.add(Box.createHorizontalStrut(125));
 		
 		Image share = FolderManager.createImage("/images/share-enabled.png", "share icon");
 		sharePane = new ImageButton(new ImageIcon(share), "Share");
@@ -242,7 +264,14 @@ public class MainFrame extends JFrame implements ActionListener, MouseListener{
                 }
             });
 		} else if(e.getSource().equals(sharePane.getIconPane())) {
-			
+			if(!inMeeting) {
+				SwingUtilities.invokeLater(new Runnable() {
+		            @Override
+		            public void run() {
+		                master.openWindowSelect();
+		            }
+		        });
+			}
 		}
 	}
 
diff --git a/TeamScreen/src/com/aptitekk/TeamScreen/Frames/WindowSelect.java b/TeamScreen/src/com/aptitekk/TeamScreen/Frames/WindowSelect.java
index 6bfe4a2..3962a2d 100644
--- a/TeamScreen/src/com/aptitekk/TeamScreen/Frames/WindowSelect.java
+++ b/TeamScreen/src/com/aptitekk/TeamScreen/Frames/WindowSelect.java
@@ -70,7 +70,7 @@ public class WindowSelect extends JFrame implements MouseListener, KeyListener{
 				//if(WindowUtil.getTopWindow().equals(w)) {
 					if(w.getRect()[0] <= x && w.getRect()[2] >= x) {
 						if(w.getRect()[1] <= y && w.getRect()[3] >= y) {
-							TeamScreenDaemon.openPushSelect(w);
+							master.openPushSelect(w);
 							System.out.println(w.getTitle());
 							break;
 						}
diff --git a/TeamScreen/src/com/aptitekk/TeamScreen/TeamScreenDaemon.java b/TeamScreen/src/com/aptitekk/TeamScreen/TeamScreenDaemon.java
index f52da3b..e1ba45a 100644
--- a/TeamScreen/src/com/aptitekk/TeamScreen/TeamScreenDaemon.java
+++ b/TeamScreen/src/com/aptitekk/TeamScreen/TeamScreenDaemon.java
@@ -244,8 +244,7 @@ public class TeamScreenDaemon implements KeyListener, HotkeyListener{
                 	SwingUtilities.invokeLater(new Runnable() {
     		            @Override
     		            public void run() {
-    		                wSelect = new WindowSelect(TSDaemon);
-    		                wSelect.setVisible(true);
+    		            	openWindowSelect();
     		            }
     		        });
                 } else if ("About".equals(item.getLabel())) {
@@ -337,20 +336,12 @@ public class TeamScreenDaemon implements KeyListener, HotkeyListener{
 		if(enabled)
 			if (identifier == 1) {
 		        //User wants to share :D
-				
-				// Create the GUI on the event-dispatching thread
-		        SwingUtilities.invokeLater(new Runnable() {
-		            @Override
-		            public void run() {
-		                wSelect = new WindowSelect(TSDaemon);
-		                wSelect.setVisible(true);
-		            }
-		        });
+				openWindowSelect();
 		    }
 		
 	}
 	
-	public static void openPushSelect(final WindowInfo window) {
+	public void openPushSelect(final WindowInfo window) {
 		SwingUtilities.invokeLater(new Runnable() {
             @Override
             public void run() {
@@ -360,6 +351,16 @@ public class TeamScreenDaemon implements KeyListener, HotkeyListener{
             }
         });
 	}
+	public void openWindowSelect() {
+		// Create the GUI on the event-dispatching thread
+        SwingUtilities.invokeLater(new Runnable() {
+            @Override
+            public void run() {
+                wSelect = new WindowSelect(TSDaemon);
+                wSelect.setVisible(true);
+            }
+        });
+	}
 	
 	public static WindowManager getWindowManager() {
 		return wManager;
-- 
2.39.5 (Apple Git-154)

